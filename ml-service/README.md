# FitPredict AI ML Service

Run `pip install -r requirements.txt`, then `python train_model.py`, then `python app.py`.

The training script generates `dataset/fat_loss_dataset.csv`, trains Linear Regression, Decision Tree, and Random Forest regressors, writes model reports, and saves `model.pkl` plus `preprocessor.pkl`.

